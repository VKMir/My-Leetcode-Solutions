class Solution {
public:
    vector<int> majorityElement(vector<int>& nums) {
        int n = nums.size();
        vector<int> ans;
        unordered_map<int, int> mmap;
        for(int i = 0; i < n; i++){
            ++mmap[nums[i]];
        }
        for(auto& it: mmap){
            if(it.second > (n/3)){
                ans.push_back(it.first);
            }
        }
        return ans;
    }
};


Time Complexity: O(n)
Space Complexity: O(k) [space complexity]

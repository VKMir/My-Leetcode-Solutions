class Solution {
public:
    string removeOuterParentheses(string s) {
        int n = s.length();
        string ans = "";
        int start = 0;
        int count = 0;
        for(int i = 0; i < n; i++){
            if(s[i] == '(') ++count;
            else --count;
            if(count == 0){
                for(int j = start + 1; j < i; j++) ans += s[j];
                start = i + 1;
            }
        }
        return ans;
    }
};


Time Complexity: O(n)
Space Complexity: O(n)

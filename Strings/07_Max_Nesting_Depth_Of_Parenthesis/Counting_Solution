class Solution {
public:
    int maxDepth(string s) {
        int n = s.length();
        int max_depth = 0;
        int depth = 0;
        for(int i = 0; i < n; i++){
            if(s[i] == '('){
                ++depth;
                max_depth = max(depth, max_depth);
            }else if(s[i] == ')'){
                --depth;
            }else{
                continue;
            }
        }
        return max_depth;
    }
};


Time Complexity: O(n)
Space Complexity: O(1)

class Solution {
public:
    bool isZeroPresent(int n){
        while(n > 0){
            int r = n % 10;
            if(r == 0) return true;
            n /= 10;
        }
        return false;
    }
    vector<int> getNoZeroIntegers(int n) {
         int x = 1, y = n - 1;
         while(isZeroPresent(x) || isZeroPresent(y)){
            ++x;
            --y;
         }
         return {x, y};
    }
};


Time Complexity: O(n log n) [worst case]
Space Complexity: O(1)
